{% include "header.njk" %}

<main class="blog-post-page">

  <div class="post-hero">
    {% if hero_emoji %}
    <span class="post-hero-emoji">{{ hero_emoji }}</span>
    {% endif %}

    {% set visibleTags = tags | displayTags %}
    {% if visibleTags | length %}
    <div class="post-tags">
      {% for tag in visibleTags %}
        <span class="post-tag">{{ tag }}</span>
      {% endfor %}
    </div>
    {% endif %}

    <h1>{{ title }}</h1>

    <div class="post-meta">
      <span>🗓 {{ date | postDate }}</span>
      <span>⏱ {{ content | readingTime }}</span>
    </div>
  </div>

  <div class="post-wrap{% if featured_image %} post-wrap--has-image{% endif %}">
    {% if featured_image %}
    <figure class="post-featured-figure">
      <img class="post-featured-image" src="{{ featured_image | mediaUrl(slug) }}" alt="{{ title }}" loading="eager" />
    </figure>
    {% endif %}

    <a class="post-back" href="/blog/">← Back to all posts</a>

    <article class="blog-post-body">
      {{ content | safe }}
    </article>

    {% set related = collections.posts | relatedPosts(page.url, visibleTags, 3) %}
    {% if related and related | length %}
    <div class="related-posts">
      <h3>You might also like</h3>
      <div class="related-grid">
        {% for post in related %}
        <a class="related-card" href="{{ post.url }}">
          <div class="related-card-date">{{ post.date | postDate }}</div>
          <div class="related-card-title">{{ post.data.title }}</div>
        </a>
        {% endfor %}
      </div>
    </div>
    {% endif %}
  </div>

  <div class="blog-cta">
    <h2>Ready to strengthen your security?</h2>
    <p>Talk to Supreme Warrior — one accountable partner for physical, electronic and cyber security.</p>
    <a class="btn-primary" href="/enquiry.html">Book a Free Assessment</a>
  </div>

</main>

{% include "footer.njk" %}
